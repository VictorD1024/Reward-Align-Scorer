[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "raise-scorer"
version = "0.1.0"
description = "RAISE — Reward-Aligned Agent Trajectory Scorer for fast, interpretable semantic reward in long-response RL training."
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }
authors = [
  { name = "Victor" }
]
keywords = [
  "rlhf",
  "grpo",
  "verl",
  "reward-model",
  "llm-as-judge",
  "semantic-alignment",
  "post-training"
]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Intended Audience :: Developers",
  "Intended Audience :: Science/Research",
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.9",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Topic :: Scientific/Engineering :: Artificial Intelligence"
]
dependencies = [
  "torch>=2.0",
  "transformers>=4.38"
]

[project.urls]
Homepage = "https://github.com/VictorD1024/RAISE"
Repository = "https://github.com/VictorD1024/RAISE"
Issues = "https://github.com/VictorD1024/RAISE/issues"
Changelog = "https://github.com/VictorD1024/RAISE/releases"

[project.optional-dependencies]
dev = [
  "pytest>=7.0",
  "ruff>=0.5"
]
sentence-transformers = [
  "sentence-transformers>=2.7"
]

[tool.setuptools.packages.find]
where = ["."]
include = ["raise_scorer*"]

[tool.ruff]
line-length = 120
target-version = "py39"
